apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: python-rest-api
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: python-rest-api
  policy:
    semver:
      range: "^0.x.0"
